# secken-private-install

A shell for install `lnmp` and `secken-private-system`

## Usage

```
$ wget "https://raw.githubusercontent.com/Secbone/secken-private-install/master/private_install.sh" && bash private_install.sh
```
